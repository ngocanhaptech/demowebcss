Welcome to this video on essential design patterns for AI systems.
In this video, you'll explore three core LLM workflow patterns, sequential or prompt
chaining, routing, and parallelization.
You'll see each pattern demonstrated with LandGraph code examples.
Along the way, you'll interpret how state variables, nodes, and workflow execution flow
work together.
And finally, you'll review the outputs generated by each pattern from sample inputs.
This video focuses on text input and output using LLMs, but you can easily modify this
using agents and tool-based outputs.
The sequential pattern, or prompt chaining, is the simplest.
You can pass one LLM's output directly into the next.
It works well because agents handle specialized tasks better, and it breaks complex problems
into manageable steps.
Consider the task of writing a cover letter based on a job description.
A technical expert drafts the resume summary, then passes it to a professional writer to
craft the letter.
Replace those people with agents, and you have prompt chaining.
Next, explore the prompt chaining code, starting with library imports and LLM initialization.
Create a chain state object and refer to this table to understand how state variables progress
through the graph.
The fields are job underscore description, resume underscore summary, and cover underscore
letter.
The generate underscore resume underscore summary function reads the job description
and summarizes key qualifications.
First, create a prompt using the job description as input.
You can also import a prompt template with the system message and variable placeholders
and chain it to the LLM.
Then, call the LLM and update the resume underscore summary key value.
The generate underscore cover underscore letter function creates the second specialized
agent in the workflow.
It builds a prompt using both resume underscore summary and job underscore description from
the current state.
Then, it calls the LLM to generate a tailored cover letter and updates the cover underscore
letter key value, completing the workflow chain.
Finally, create the workflow graph using chain state as input, add the generate resume summary
node, add the cover letter node, and connect them sequentially.
Next, set entry and endpoints and compile the graph.
Complete the job description input state, run the workflow, and display the state variable
as a table with the job description filled in.
The resume summary agent generates a summary, which becomes input for the cover letter agent.
The cover letter agent takes the resume summary and job description.
The cover letter agent outputs a cover letter, which populates the cover letter variable.
From there, you can use it however you like.
Next, use the routing design pattern to build a system that intelligently selects the right
agent for a task.
You'll create a routing agent that analyzes the query and decides whether to summarize
or translate the input.
The router converts the LLM's output to a string and routes it to a conditional node,
which directs the flow to the appropriate agent node.
The response is stored in a state variable.
While it doesn't explicitly pass through, the end node is labeled as the output for
clarity.
Next, create the graph.
First, define the router state with three fields to track the user input, task type,
and final output.
The table shows the state variables, fields in the first column, values in the second.
Create a pedantic data schema so that the LLM outputs either summarize or translate
and bind it to the LLM.
This will be used in LLM underscore router.
First, define the router node.
When defining the router node, the input and output are router state.
The prompt includes instructions to translate or summarize.
User input is added to the message, the LLM is called, and the tool call output, either
translate or summarize, is extracted and passed to task underscore type.
This function takes the router node's output, extracts the task type, and returns a string
for add underscore conditional underscore edges.
This is a conditional edge, not really a node.
Next, define the summarize and translate nodes.
First, define the summarize node.
The prompt instructs the LLM to summarize the user input.
The node is selected by the router, the LLM is called, and the result is stored in the
output field.
Define the translate node.
The prompt instructs to translate the user input.
The node is selected by the router, the LLM is called, and the result is stored in the
output field.
Finally, create the state graph object workflow.
Add the router node, add the summarization node, add the translation node, set router
underscore node as the entry point.
Next, add the router to router underscore node.
If the output is summarize, route to the summarize node, if translate, route to the translate
node.
Finally, add two finish points, analogous to end nodes.
Transform the workflow graph into an executable length graph application.
Entering the user's translation request triggers the full workflow and stores the final output.
Use this table to define router state.
The task type is translate, which updates the router state.
This state is passed to the router function, which routes it to the translate node.
The translate node outputs the translated text.
This text is stored in the output key of the router's state.
If the input requests the summary instead, the application generates the appropriate
summary.
The parallelization pattern runs multiple LLM tasks simultaneously instead of sequentially,
boosting speed and throughput.
It's like a kitchen with multiple chefs.
Chopping vegetables, boiling pasta, baking bread, an aggregator or head chef combines
everything together.
In AI workflows, you split problems into independent parts, process them in parallel, and then
merge.
Next, build this pattern and length graph.
Define a state container with text fields for the original English input, language fields
for each translation, and combined underscore output to merge translations.
This will be represented as a table.
Create a translation node for each target language, for example, the French node.
The prompt is translate to French.
The input is the text field from the state variable.
Output is stored in the French key values.
In addition, here are the Spanish and Japanese nodes.
The aggregator node takes the input state variable and then extracts text, French, Spanish,
and Japanese from the state, merges them into one string, and stores it in combined underscore
output.
Initialize a new state graph for the parallel workflow.
Add three translation nodes, one for each language, running in parallel.
Then add the aggregator node.
Each translation node needs direct access to the initial state.
Connect all three nodes to start with three edges, allowing parallel execution.
Each translation node connects to the aggregator to send its results for final combination.
An edge is added from the aggregator to the end node and the graph is compiled.
Enter an English sentence in the text field and run the workflow.
This triggers all translation nodes to run in parallel.
In the final step, the aggregator takes the outputs from all translation nodes and combines
them.
In this video, you learned that the sequential or prompt chaining pattern passes one LLM's
output as the next LLM's input, breaking complex tasks into manageable steps.
The routing pattern uses a router agent to analyze input, determine a task type, and
direct execution to the correct agent node.
The parallelization pattern runs multiple independent LLM tasks simultaneously and merges
their outputs with an aggregator node.
LineGraph implements each pattern using state variables, nodes, and directed edges, allowing
structured workflows for clear execution flow and outputs.